<div class="card">
    <% @idiom.comments.each do |comment|%>
    <div class="show__body">
        <%=comment.body%>
    </div>
        <div class="card__bottom">
            <div class="show__links">
                <% if current_user != nil && current_user.is_admin? %>
                    <%= link_to 'Supprimer', idiom_comment_path(@idiom.id, comment.id), method: 'DELETE' %>&nbsp;/&nbsp;
                    <%= link_to 'Modérer', edit_idiom_comment_path(@idiom.id, comment.id)%><br>
                <% end %>
                <% if current_user != nil && comment.user == current_user && !current_user.is_admin? %>
                    <%= link_to 'Supprimer', idiom_comment_path(@idiom.id, comment.id), method: 'DELETE' %>&nbsp;/&nbsp;
                    <%= link_to 'Modifier', edit_idiom_comment_path(@idiom.id, comment.id) %><br>
                <% end %>
            </div>
            <div class="right">
                <%= image_tag comment.user.avatar, class: 'pic-avatar-com' %>
                Par&nbsp;<b><%=comment.user.alias%></b>, le&nbsp;<%=comment.created_at.strftime("%d")%>&nbsp;<%=comment.created_at.strftime("%b.")%>&nbsp;<%=comment.created_at.strftime("%Y")%>
            </div>
        </div>
    <%end%>
</div> 
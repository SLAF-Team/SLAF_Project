
<div class="navbar">
  <div class="navbar-top">
    <img id="navbar-logo">
    <%= link_to "Accueil", root_path %>
    <div class="navbar-search">
      <%= form_for "", url: idioms_path, role: "search", method: :get do%>
      <%= text_field_tag :search, @search, placeholder: "Recherche une expression"%>
      <% end%>
      </div>
    </div>
    <div class="navbar-browse">
      <h2>Browse<h2>
      <%# Caler un dropdown %>
      <ul class="navbar-browse-index">
        <% @letters.each do |letter| %>
        <br><%= link_to letter, idioms_path(:search => letter) %>
        <% end %>
      </ul>
    </div>
    <div class="navbar-buttons">
      <btn><%= render 'application/random' %></btn>

      <%= link_to "Top", idioms_path(:search => "top") %>
    </div>
  </div>
  <div class="navbar-bottom">
    <% if user_signed_in? %>
      <%= link_to "Profil", user_path(current_user.id) %>
      <%= link_to "Se déconnecter", destroy_user_session_path, :method => :delete %><br>
      <%if current_user.admin == true%>
        <%=link_to 'Admin dashboard', admin_index_path%>
      <%end%>
    <% else %>
      <%= link_to "S'inscrire", new_user_registration_path %>
      <%= link_to "Se connecter", new_user_session_path %>
    <% end %>
  </div>
</div>